string = "radar"
is_palindrome = True
for i in range(len(string)//2):
    if string[i] != string[-i-1]:
        is_palindrome = False
        break
print(string, "is a palindrome" if is_palindrome else "is not a palindrome")