def find_max(numbers):
    max_num = numbers[0]
    for num in numbers:
        if num > max_num:
            max_num = num
    return max_num

numbers_list = [10, 5, 7, 20, 15]
max_number = find_max(numbers_list)
print(max_number)